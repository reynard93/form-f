import createInputComponent from '../../utils/create-input-component'

const SchemaTable = createInputComponent('mom-table', {
  newProps: {
    ignoreInputState: false
  }
})

export default SchemaTable
